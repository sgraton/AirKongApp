/**
 * @format
 */

import {AppRegistry} from 'react-native';
import AirGodzillaApp from './src/index';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => AirGodzillaApp);
